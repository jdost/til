#!/bin/sh

source $(til import lib)
SEARCH_DIR=$(echo $DOCS_ROOT | sed 's/\.\.\// /g' | cut -d' ' -f2)

case "$1" in
   "-p") SEARCH_DIR+="/projects"; shift;;
   "-d") SEARCH_DIR+="/201*"; shift;;
   "-t") SEARCH_DIR+="/topics"; shift;;
esac

ack $* $SEARCH_DIR

#!/bin/sh

DEFAULT="# "

source $(til import lib)
ENTRY=$DOCS_ROOT/diary/$(date +"%Y/%m")/$(date +"%d").$DOC_SUFFIX
HEADER="${1:-$DEFAULT} $(date +"%A - %D")"

init_file $ENTRY

if [[ ! -s $ENTRY ]]; then
   echo "$HEADER" > $ENTRY
fi

_vim $ENTRY
git add $ENTRY
